# Documentation: https://simplelocalize.io/docs/integrations/github-app/
# You can use {lang} and {ns} placeholders in paths

upload:
  when:
    push:
  files:
    - path: "lib/i18n/translations/english.yaml" # Path to your source translations
      format: "yaml" # Input file format
      language-key: "en" # Input language key
      options:
        - REPLACE_TRANSLATION_IF_FOUND
        - DEPRECATE_NOT_PRESENT_KEYS
        - ACTIVATE_PRESENT_KEYS

download:
  files:
    - path: "lib/i18n/translations/{lang}.yaml" # Output translations
      format: "yaml" # Output file format
      sort: "LEXICOGRAPHICAL"
      options:
        - "EXCLUDE_DEPRECATED_KEYS"

pull-request:
